js:let res={};let d=[];let regexp = /<p>|<\/p>/g;let paragraph = '　　';let html = request('https://meiriyiwen.com/random/iphone');let list = parseDomForArray(html,'.articleContent&&p');for(let i=0;i<10;i++){d.push({col_type: "blank_block"})}d.push({title: '““””<h4>'+parseDomForHtml(html,'.articleTitle&&h2&&Text')+'</h4>'+'““”” <small><font color=grey>'+parseDomForHtml(html,'.articleAuthorName&&Text')+'</font> </small>',url: 'hiker://empty',col_type: 'text_center_1'})
d.push({title:(paragraph+list.join('\n\n'+paragraph)).replace(regexp, ''),col_type: 'long_text'});for(let i=0;i<50;i++){d.push({col_type: "blank_block"})}res.data=d;setHomeResult(res);